use crate::CommandType;
use std::fs::File;
pub struct CodeWriter {}
impl CodeWriter {
    pub fn new(output_file: File) {}
    fn writeArithmetic(command: String) {}
    fn writePushPop(command: CommandType) {}
    fn close() {}
}
